// import request from 'superagent'

// const bookingUrl = '/api/v1/bookings'

// export async function addBooking(booking) {
//   const addedBooking = await request.post(bookingUrl).send(booking)
//   return addedBooking.body
// } 